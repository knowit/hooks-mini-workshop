import { useState } from "react";

export default initialValue => {
  // Use useState to manage TodoForm's state

  // Return the current value, a method for changing the value, and a reset function
  return {
    // value,
    // onChange,
    // reset,
  };
};
