import { useState } from "react";

export default initialValue => {
  // use useState to manage the list of todos

  /**
   * Implement the following functions/values:
   * todos: the current list of todos
   * addTodo: method for adding a new todo (string)
   * deleteTodo: method for deleteting a todo by index
   * */

  return {
    //  todos,
    //  addTodo,
    //  deleteTodo,
  };
};
